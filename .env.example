# مصدر البيانات: اترك القيمة فارغة (أو false) لاستخدام البيانات الوهمية (MockRepository).
# اضبطها على true لاستخدام واجهة REST الحقيقية (HttpRepository) بعد جاهزية خلفية Spring Boot.
VITE_USE_API=false

# عنوان خلفية Spring Boot (يُستخدم فقط عندما VITE_USE_API=true)
VITE_API_BASE_URL=http://localhost:8080/api
